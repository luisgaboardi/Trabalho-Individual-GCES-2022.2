version: "3.9"
name: trabalho-individual
services:
  mongodb:
    container_name: mongodb
    image: mongo:4.4.3-bionic
    env_file:
      - .env
    command: --auth
    ports:
      - 27017:27017
    volumes:
      - mongodb:/db

volumes:
  mongodb: